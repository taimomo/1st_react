{
  "name": "1st_react",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "postCreateCommand": "if [ ! -f package.json ]; then npm create vite@latest . -- --template react; fi && npm install",
  "forwardPorts": [
    5173
  ],
  "remoteUser": "node"
}